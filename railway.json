{
  "build": {
    "buildCommand": "npm install && apt-get update && apt-get install -y python3 python3-pip && curl -L https://github.com/yt-dlp/yt-dlp/releases/latest/download/yt-dlp -o /usr/bin/yt-dlp && chmod a+rx /usr/bin/yt-dlp"
  },
  "deploy": {
    "startCommand": "node index.js"
  }
}